# Core
openai>=1.30          # OpenAI-compatible client (works with GMI Cloud, OpenAI, etc.)
requests>=2.31
feedparser>=6.0        # RSS/Atom parsing
beautifulsoup4>=4.12   # strip HTML out of RSS summaries; parse og:image tags
python-dotenv>=1.0     # load .env for local runs
pytz>=2024.1           # America/Los_Angeles date handling

# PDF rendering (two-column magazine layout)
markdown>=3.5          # Markdown -> HTML
weasyprint>=61         # HTML + CSS -> PDF (needs pango/cairo system libs on CI)
matplotlib>=3.8        # watchlist "biggest movers" chart
Pillow>=10.0           # image handling for charts/photos
pypdf>=4.0             # read the PDF back to check WeasyPrint kept all the text

# Market data (best-effort; failures are non-fatal)
yfinance>=0.2.40
