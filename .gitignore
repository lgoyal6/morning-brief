# Secrets & local env
.env
.env.*

# Logs / OS / Python cruft
*.log
__pycache__/
*.pyc
.DS_Store
.venv/
venv/
.python-version

# Per-run provenance: uploaded as workflow artifacts, but not committed to git.
briefs/*-sources.json
briefs/.brief_state.json
briefs/*.debug.html

# Generated briefs (the dated .md/.pdf and latest PDF) ARE committed on purpose.

# Cloudflare Worker (trigger/) build + local state
trigger/node_modules/
trigger/.wrangler/
trigger/.dev.vars
